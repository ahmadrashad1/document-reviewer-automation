# n8n snippets and debug notes (archived)

These files are **not required** to run the app. They were used while building the RAG pipeline (chunking, embeddings, Chroma, merge nodes).

- **`n8n-*.js`** — Code snippets you can paste into n8n Code nodes (e.g. merge chunk + embedding, format Chroma request).
- **`n8n-*.md`** — Fix and debug notes (Chroma JSON, query passthrough, etc.).
- **`My workflow.json`** — Old n8n export (webhook path `document-analysis`). Use **`n8n-workflow-document-reviewer.json`** in the project root instead.

The active workflow is **`n8n-workflow-document-reviewer.json`** in the project root.
